#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#include "aux.h"


int soma(int n1, int n2);

int retornaV_absoluto(int num);

void dec2hex(int num_d);

float calcula_percent(float v_real, int percentual);

float grauEscola(float con_Esp, float con_Ger, float mat, float port);
float valorTotal(int cod);

int soma_dig(int num);
char* retornaSenha(int dia, int mes, int ano);

int conv_HemM(int horario);
void exibeTempo(int tempoInicio, int tempoFim);

float retornaVolumeCaixa(int lado_A, int lado_B, int altura);
float retornaVolumeCilindro(int raio, int altura);
float volumeFigura(int lado_A, int lado_B, int raio, int altura);

float distancia_Pontos(float x1, float y1, float x2, float y2);


int produto_vencido(int dia_visita, int mes_visita, int ano_visita, int dia_venc, int mes_venc, int ano_venc);
float calcula_multa(int qtd_Prod_conferidos, int qtd_Prod_F_val);







int main(void)
{

	/*
	1)

	int n1 = 6;
	int n2 = 4;

	printf("Soma ---> %d + %d = %d\n\n", n1, n2, soma(n1, n2));



	return 0;*/


	//=======================================================================

	//2) 

	/*int num;
	int v_Abs;
	printf("Digite um numero interio positivou ou negativo: ");
	scanf("%d", &num);

	printf("\n");


	v_Abs = retornaV_absoluto(num);
	printf("O valor absoluto do numero %d eh = %d ", num, v_Abs);*/

	//======================================================================

	//3)

	//float g_far;
	//float g_cel;

	//printf("Digite uma temperatura em fahrenheit: ");
	//scanf("%f", &g_far);

	//g_cel = f2c(g_far);

	//printf("A conversao de %.2f fahrenheit para celcius eh: %.2f", g_far, g_cel);

	//==============================================================================

	//4)

	/*dec2hex(345);*/

	//==============================================================================

	//5)

	//float num;
	//int percent;
	//float percentual;

	//printf("Digite um numero real: ");
	//scanf("%f", &num);

	//printf("Qual a porcentagem desejada: ");
	//scanf("%d", &percent);

	//percentual = calcula_percent(num, percent);

	//printf("%d/100 de %.2f = %.2f\n", percent, num, percentual);

	//return 0;

	//==============================================================================


	//6) 

	/*float con_Esp;
	float con_Ger;
	float mat;
	float port;
	float grauFinal;

	printf("Digite a nota de conhecimento especifico: ");
	scanf("%f", &con_Esp);

	printf("Digite a nota de conhecimento geral: ");
	scanf("%f", &con_Ger);

	printf("Digite a nota de matematica: ");
	scanf("%f", &mat);

	printf("Digite a nota de portugues: ");
	scanf("%f", &port);

	grauFinal = grauEscola(con_Esp, con_Ger, mat, port);

	printf("O grau final do aluno eh: %.2f", grauFinal);*/

	//==============================================================================

	//7)


	/*int cod;
	
	printf("Digite o codigo do produto: ");
	scanf("%d", &cod);

	printf("\n");

	printf("O valor total a pagar pelo produto eh: %.2f", valorTotal(cod));

	return 0;*/

	//==============================================================================

	/*char* senha = retornaSenha(27, 3, 1999);

	printf("Senha gerada: %s\n", senha);
	return 0;*/

	//==============================================================================

	//9)
	
	/*int hrInicio;
	int hrFinal;

	int tempoFinal_Minutos;

	printf("Digite o horario de inicio: ");
	scanf("%d", &hrInicio);

	printf("Digite o horario de termino: ");
	scanf("%d", &hrFinal);

	int hr_Inicio_min = conv_HemM(hrInicio);

	int hr_Final_min = conv_HemM(hrFinal);


	exibeTempo(hr_Inicio_min, hr_Final_min);*/

//============================================================================================


	//10)

	/*int dias;
	printf("Digite a quantidade de dias: ");
	scanf("%d", &dias);

	printf("\n");

	exibeEmSemanas(dias);

	//============================================================================================

//11) 

	/*int altura;
	int lado_A;
	int lado_B;
	int raio;

	printf("Digite a altura da figura: ");
	scanf("%d", &altura);

	printf("\n");

	printf("Digite o lado A da figura: ");
	scanf("%d", &lado_A);

	printf("\n");

	printf("Digite o lado B da figura: ");
	scanf("%d", &lado_B);

	printf("\n");

	printf("Digite o raio da figura: ");
	scanf("%d", &raio);

	printf("\n");

	float vol = volumeFigura(lado_A, lado_B, raio, altura);

	printf("O volume da figura eh: %.2f", vol);

	return 0;*/

	//============================================================================================

	//16)

	/*float xA, yA, xB, yB, xC, yC;

	printf("Digite as coordenadas do potno A: ");
	scanf("%f %f", &xA, &yA);

	printf("Digite as coordenadas do potno B: ");
	scanf("%f %f", &xB, &yB);

	printf("Digite as coordenadas do potno C: ");
	scanf("%f %f", &xC, &yC);

	float dist_AB = distancia_Pontos(xA, yA, xB, yB);
	float dist_BC = distancia_Pontos(xB, yB, xC, yC);
	float dist_CA = distancia_Pontos(xC, yC, xA, yA);	

	float perimetro = dist_AB + dist_BC + dist_CA;

	printf("O perimetro do triangulo formado pelos pontos A, B e C eh: %.2f", perimetro);

	return 0;*/


	//======================================================================================

	//17) Faça um programa em C que inicialmente capture a data da visita ao supermercado.Em seguida, para
	//cada produto conferido, capture o código do produto(um número inteiro) e a data do seu
	//vencimento, mostrando para cada produto se ele está ou não na validade.A entrada de dados é
	//finalizada quando for digitado um código de produto negativo.Seu programa deverá então calcular e
	//exibir o valor da multa, caso haja, ou informar que o supermercado está isento de multas.
	//Observação: Seu programa deverá utilizar as funções definidas nos dois primeiros itens desta
	//questão.

	/*int dia_visita;
	int mes_visita;
	int ano_visita;

	int dia_venc;
	int mes_venc;
	int ano_venc;

	float verf_produto_vencido;

	int cod_Produto;

	int qtd_Produto;
	int qtd_Prod_F_Val = 0;

	float valor_multa;

	printf("Digite o codigo do produto: ");
	scanf("%d", &cod_Produto);
	
	printf("\n");

	while (cod_Produto >= 0) {

		printf("Digite a data da visita ao supermercado dd/mm/aaaa: ");
		scanf("%d/%d/%d", &dia_visita, &mes_visita, &ano_visita);

		printf("\n");

		printf("Digite a quantidade do produto a verificar: ");
		scanf("%d", &qtd_Produto);

		printf("\n");


		for (int i = 0; i < qtd_Produto; i++) {

			printf("Digite a data de validade do produto dd/mm/aaaa: ");
			scanf("%d/%d/%d", &dia_venc, &mes_venc, &ano_venc);

			verf_produto_vencido = produto_vencido(dia_visita, mes_visita, ano_visita, dia_venc, mes_venc, ano_venc);

			if (verf_produto_vencido == 1) {
				printf("O Produto #%d de codigo %d esta FORA da validade\n", i+1, cod_Produto);
				qtd_Prod_F_Val += 1;
				printf("\n");
			}
			else {
				printf("O Produto #%d de codigo %d esta DENTRO da validade\n",i+1, cod_Produto);
				printf("\n");
			}
		}

		printf("\n");

		printf("Digite o codigo do produto: ");
		scanf("%d", &cod_Produto);

		printf("\n");
		printf("\n");

		valor_multa = calcula_multa(qtd_Produto, qtd_Prod_F_Val);

		printf("O estabelecimento teve %d produtos fora da validade e deve pagar uma multa de %.2fR$", qtd_Prod_F_Val, valor_multa);

	}
	
	return 0;
	*/
}










#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#include "aux.h"


int main(void)
{

	/*
	1)

	int n1 = 6;
	int n2 = 4;

	printf("Soma ---> %d + %d = %d\n\n", n1, n2, soma(n1, n2));



	return 0;*/


	//=======================================================================

	//2) 

	/*int num;
	int v_Abs;
	printf("Digite um numero interio positivou ou negativo: ");
	scanf("%d", &num);

	printf("\n");


	v_Abs = retornaV_absoluto(num);
	printf("O valor absoluto do numero %d eh = %d ", num, v_Abs);*/

	//======================================================================

	//3)

	//float g_far;
	//float g_cel;

	//printf("Digite uma temperatura em fahrenheit: ");
	//scanf("%f", &g_far);

	//g_cel = f2c(g_far);

	//printf("A conversao de %.2f fahrenheit para celcius eh: %.2f", g_far, g_cel);

	//==============================================================================

	//4)

	/*dec2hex(345);*/

	//==============================================================================

	//5)

	//float num;
	//int percent;
	//float percentual;

	//printf("Digite um numero real: ");
	//scanf("%f", &num);

	//printf("Qual a porcentagem desejada: ");
	//scanf("%d", &percent);

	//percentual = calcula_percent(num, percent);

	//printf("%d/100 de %.2f = %.2f\n", percent, num, percentual);

	//return 0;

	//==============================================================================


	//6) 

	/*float con_Esp;
	float con_Ger;
	float mat;
	float port;
	float grauFinal;

	printf("Digite a nota de conhecimento especifico: ");
	scanf("%f", &con_Esp);

	printf("Digite a nota de conhecimento geral: ");
	scanf("%f", &con_Ger);

	printf("Digite a nota de matematica: ");
	scanf("%f", &mat);

	printf("Digite a nota de portugues: ");
	scanf("%f", &port);

	grauFinal = grauEscola(con_Esp, con_Ger, mat, port);

	printf("O grau final do aluno eh: %.2f", grauFinal);*/

	//==============================================================================

	//7)


	/*int cod;
	
	printf("Digite o codigo do produto: ");
	scanf("%d", &cod);

	printf("\n");

	printf("O valor total a pagar pelo produto eh: %.2f", valorTotal(cod));

	return 0;*/

	//==============================================================================

	/*char* senha = retornaSenha(27, 3, 1999);

	printf("Senha gerada: %s\n", senha);
	return 0;*/

	//==============================================================================

	//9)
	
	/*int hrInicio;
	int hrFinal;

	int tempoFinal_Minutos;

	printf("Digite o horario de inicio: ");
	scanf("%d", &hrInicio);

	printf("Digite o horario de termino: ");
	scanf("%d", &hrFinal);

	int hr_Inicio_min = conv_HemM(hrInicio);

	int hr_Final_min = conv_HemM(hrFinal);


	exibeTempo(hr_Inicio_min, hr_Final_min);*/

//============================================================================================


	//10)

	/*int dias;
	printf("Digite a quantidade de dias: ");
	scanf("%d", &dias);

	printf("\n");

	exibeEmSemanas(dias);

	//============================================================================================

//11) 

	/*int altura;
	int lado_A;
	int lado_B;
	int raio;

	printf("Digite a altura da figura: ");
	scanf("%d", &altura);

	printf("\n");

	printf("Digite o lado A da figura: ");
	scanf("%d", &lado_A);

	printf("\n");

	printf("Digite o lado B da figura: ");
	scanf("%d", &lado_B);

	printf("\n");

	printf("Digite o raio da figura: ");
	scanf("%d", &raio);

	printf("\n");

	float vol = volumeFigura(lado_A, lado_B, raio, altura);

	printf("O volume da figura eh: %.2f", vol);

	return 0;*/

	//============================================================================================

	//16)

	/*float xA, yA, xB, yB, xC, yC;

	printf("Digite as coordenadas do potno A: ");
	scanf("%f %f", &xA, &yA);

	printf("Digite as coordenadas do potno B: ");
	scanf("%f %f", &xB, &yB);

	printf("Digite as coordenadas do potno C: ");
	scanf("%f %f", &xC, &yC);

	float dist_AB = distancia_Pontos(xA, yA, xB, yB);
	float dist_BC = distancia_Pontos(xB, yB, xC, yC);
	float dist_CA = distancia_Pontos(xC, yC, xA, yA);	

	float perimetro = dist_AB + dist_BC + dist_CA;

	printf("O perimetro do triangulo formado pelos pontos A, B e C eh: %.2f", perimetro);

	return 0;*/


	//======================================================================================

	//17) Faça um programa em C que inicialmente capture a data da visita ao supermercado.Em seguida, para
	//cada produto conferido, capture o código do produto(um número inteiro) e a data do seu
	//vencimento, mostrando para cada produto se ele está ou não na validade.A entrada de dados é
	//finalizada quando for digitado um código de produto negativo.Seu programa deverá então calcular e
	//exibir o valor da multa, caso haja, ou informar que o supermercado está isento de multas.
	//Observação: Seu programa deverá utilizar as funções definidas nos dois primeiros itens desta
	//questão.

	/*int dia_visita;
	int mes_visita;
	int ano_visita;

	int dia_venc;
	int mes_venc;
	int ano_venc;

	float verf_produto_vencido;

	int cod_Produto;

	int qtd_Produto;
	int qtd_Prod_F_Val = 0;

	float valor_multa;

	printf("Digite o codigo do produto: ");
	scanf("%d", &cod_Produto);
	
	printf("\n");

	while (cod_Produto >= 0) {

		printf("Digite a data da visita ao supermercado dd/mm/aaaa: ");
		scanf("%d/%d/%d", &dia_visita, &mes_visita, &ano_visita);

		printf("\n");

		printf("Digite a quantidade do produto a verificar: ");
		scanf("%d", &qtd_Produto);

		printf("\n");


		for (int i = 0; i < qtd_Produto; i++) {

			printf("Digite a data de validade do produto dd/mm/aaaa: ");
			scanf("%d/%d/%d", &dia_venc, &mes_venc, &ano_venc);

			verf_produto_vencido = produto_vencido(dia_visita, mes_visita, ano_visita, dia_venc, mes_venc, ano_venc);

			if (verf_produto_vencido == 1) {
				printf("O Produto #%d de codigo %d esta FORA da validade\n", i+1, cod_Produto);
				qtd_Prod_F_Val += 1;
				printf("\n");
			}
			else {
				printf("O Produto #%d de codigo %d esta DENTRO da validade\n",i+1, cod_Produto);
				printf("\n");
			}
		}

		printf("\n");

		printf("Digite o codigo do produto: ");
		scanf("%d", &cod_Produto);

		printf("\n");
		printf("\n");

		valor_multa = calcula_multa(qtd_Produto, qtd_Prod_F_Val);

		printf("O estabelecimento teve %d produtos fora da validade e deve pagar uma multa de %.2fR$", qtd_Prod_F_Val, valor_multa);

	}
	
	return 0;
	*/
}






